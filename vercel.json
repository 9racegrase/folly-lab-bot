const crypto = require('crypto');

const CHANNEL_SECRET = process.env.LINE_CHANNEL_SECRET;
const ACCESS_TOKEN = process.env.LINE_ACCESS_TOKEN;

function validateSignature(body, signature) {
  const hash = crypto
    .createHmac('SHA256', CHANNEL_SECRET)
    .update(body)
    .digest('base64');
  return hash === signature;
}

async function replyMessage(replyToken, text) {
  await fetch('https://api.line.me/v2/bot/message/reply', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${ACCESS_TOKEN}`
    },
    body: JSON.stringify({
      replyToken,
      messages: [{ type: 'text', text }]
    })
  });
}

function getBotReply(msg) {
  const m = msg.toLowerCase();

  if (m.includes('手鍊')) return '可以的，請問您比較想看日常配戴款、送禮款，還是客製款呢？\n\n可以先逛逛這裡：\n👉 https://folly-lab.com/product-category/crystals-accessories/%E7%A4%A6%E7%9F%B3%E6%89%8B%E4%B8%B2';
  if (m.includes('項鍊')) return '項鍊這邊有礦石項鍊、檀木項鍊等方向，請問是自用還是送禮呢？\n\n可以先逛逛這裡：\n👉 https://folly-lab.com/product-category/crystals-accessories/%E7%A4%A6%E7%9F%B3%E9%A0%85%E9%8D%8A/';
  if (m.includes('送禮') || m.includes('禮物')) return '如果是送禮，我可以先幫您抓方向。對方風格偏溫柔、極簡、個性，還是自然儀式感呢？';
  if (m.includes('客製')) return '可以客製的 ✨\n請告訴我：\n1. 品項（手鍊／項鍊）\n2. 喜歡的晶石或顏色\n3. 預算範圍\n4. 是否送禮\n\n整理好後會轉由客服確認細節。';
  if (m.includes('淨化')) return '淨化用品推薦：\n\n🧂 喜馬拉雅黑鹽 NT$66\n🌿 秘魯聖木條 NT$55\n🪴 北美白鼠尾草 NT$88–500\n💎 白水晶碎石 NT$60–120\n\n👉 立即選購：https://folly-lab.com/product-category/tools\n\n請問是自己用還是送禮呢？';
  if (m.includes('擺件') || m.includes('擺設')) return '礦石擺件這邊有力量動物、神聖幾何等收藏款，適合空間擺設或送禮 🪨\n\n👉 立即選購：https://folly-lab.com/product-category/%E7%A4%A6%E7%9F%B3%E6%93%BA%E4%BB%B6';
  if (m.includes('咖啡') || m.includes('脈輪')) return '脈輪咖啡是我們精品咖啡品牌線，送禮自用都很合適 ☕\n\n👉 立即選購：https://folly-lab.com/product-category/zarpalanga';
  if (m.includes('tattoo') || m.includes('刺青') || m.includes('紋身')) return 'Tattoo DNA 刺青保養系列：\n\n・清潔慕斯 NT$300\n・Gold 保養霜 NT$400\n・Pink 花香保養霜 NT$300\n・Gold 保養組合 NT$700\n\n👉 立即選購：https://folly-lab.com/product-category/tattoo-dna';
  if (m.includes('訂單') || m.includes('出貨')) return '請提供訂單編號與下單姓名，我會協助轉交客服為您確認進度 📦';
  if (m.includes('預算') || m.includes('多少錢')) return '請問預算大約落在哪一段呢？\n・500 以下\n・500–1500\n・1500–3000\n・3000 以上';
  if (m.includes('真人') || m.includes('客服')) return '好的，為您轉由真人客服協助，請稍候 🙏';

  return '您好！歡迎來到 FOLLY LAB 選物助手 🌿\n\n請問您想找：\n・手鍊／項鍊\n・淨化用品\n・客製訂做\n・訂單查詢\n・聯絡真人客服';
}

export default async function handler(req, res) {
  if (req.method !== 'POST') return res.status(200).send('OK');

  const signature = req.headers['x-line-signature'];
  const rawBody = JSON.stringify(req.body);

  if (!validateSignature(rawBody, signature)) {
    return res.status(403).send('Invalid signature');
  }

  const events = req.body.events || [];

  for (const event of events) {
    if (event.type === 'message' && event.message.type === 'text') {
      const userMsg = event.message.text;
      const reply = getBotReply(userMsg);
      await replyMessage(event.replyToken, reply);
    }
  }

  res.status(200).send('OK');
}  if (m.includes('咖啡') || m.includes('脈輪')) return '脈輪咖啡是我們精品咖啡品牌線，送禮自用都很合適 ☕\n\n👉 立即選購：https://folly-lab.com/product-category/zarpalanga';
  if (m.includes('tattoo') || m.includes('刺青') || m.includes('紋身')) return 'Tattoo DNA 刺青保養系列：\n\n・清潔慕斯 NT$300\n・Gold 保養霜 NT$400\n・Pink 花香保養霜 NT$300\n・Gold 保養組合 NT$700\n\n👉 立即選購：https://folly-lab.com/product-category/tattoo-dna';
  if (m.includes('訂單') || m.includes('出貨')) return '請提供訂單編號與下單姓名，我會協助轉交客服為您確認進度 📦';
  if (m.includes('預算') || m.includes('多少錢')) return '請問預算大約落在哪一段呢？\n・500 以下\n・500–1500\n・1500–3000\n・3000 以上';
  if (m.includes('真人') || m.includes('客服')) return '好的，為您轉由真人客服協助，請稍候 🙏';

  return '您好！歡迎來到 FOLLY LAB 選物助手 🌿\n\n請問您想找：\n・手鍊／項鍊\n・淨化用品\n・客製訂做\n・訂單查詢\n・聯絡真人客服';
}

export default async function handler(req, res) {
  if (req.method !== 'POST') return res.status(200).send('OK');

  const signature = req.headers['x-line-signature'];
  const rawBody = JSON.stringify(req.body);

  if (!validateSignature(rawBody, signature)) {
    return res.status(403).send('Invalid signature');
  }

  const events = req.body.events || [];

  for (const event of events) {
    if (event.type === 'message' && event.message.type === 'text') {
      const userMsg = event.message.text;
      const reply = getBotReply(userMsg);
      await replyMessage(event.replyToken, reply);
    }
  }

  res.status(200).send('OK');
}
